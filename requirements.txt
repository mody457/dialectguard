# Runtime dependencies. Exact pins keep local, CI and container environments
# identical. Update deliberately, one bump per commit.
fastapi==0.135.2
uvicorn==0.42.0
pydantic==2.11.7
slowapi==0.1.10
transformers==5.15.1
tokenizers==0.22.2
safetensors==0.8.0
torch==2.11.0
numpy==2.4.3
huggingface-hub==1.29.0
